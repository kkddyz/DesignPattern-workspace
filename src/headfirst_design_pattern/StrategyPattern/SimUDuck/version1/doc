这么设计是一种针对实现编程，
       1我们把实现(代码)紧紧绑在类里，不方便选择实现，也无法重用实现代码。

       我们将可能变化的部分从超类中抽离，(第一个原则)
       针对接口编程，(第二原则) 就增加了灵活性，我们只需要为类增加一个接口对象，就能注入某种行为。
       将接口的实现封装在行为类中，达到组装的效果。

       针对接口编程 将做什么与怎么做分离了，接口只有做什么，实现类中有怎么做